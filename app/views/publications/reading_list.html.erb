<%if @reading_publications.length == 0%>
  <h3>No Articles or Reviews have been added to yor reading list</h3>
<%else%>
  <div id="articleInnerContainer" class="articleOuterContainer">
    <table class="articleTable">
      <tr>
        <th colspan="2"><%= "Your Reading List"%></th>
      </tr>
      <%@reading_publications.all.each do |p|%>
        <tr>
          <td class="articleImageCell">
            <%= image_tag(p.img_string, :class => "articleImageShow") %>
          </td>
          <td class="articleTitleCell">
            <p><%=link_to p.title, publication_path(p), class: "linkStyle3" %></p><br/>
            <p class="articleDescriptionCell"><%=p.intro%></p>
          </td>
        </tr>
      <%end%>
    </table>
  </div>
<%end%>

<div class="floatClearer"></div>